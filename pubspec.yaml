name: iptv_tester
description: A multi-threaded IPTV Speed Tester and Resolution Detector for Android.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.0
  path_provider: ^2.1.2
  shared_preferences: ^2.2.2
  # 核心调整：使用与您找到的 AAR 文件完全一致的 GPL 长期支持版
  ffmpeg_kit_flutter_full_gpl: ^6.0.3-LTS

flutter:
  uses-material-design: true
