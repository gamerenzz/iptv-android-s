name: iptv_tester

description: IPTV Tester

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  http: ^1.1.0
  path_provider: ^2.1.1
  shared_preferences: ^2.2.1

  ffmpeg_kit_flutter_min_gpl: ^6.0.3

flutter:
  uses-material-design: true
